This module adds a new security group "User: Inventory Reporting Access" that grants
access to the Inventory > Reporting menu and read-only access to stock
valuation layers (if ``stock_account`` is installed).

By default, only users with the Inventory Administrator role can access the
Reporting menu and stock valuation data. This module allows you to grant
reporting access to regular Inventory Users without giving them full
administrator privileges.
